<!-- ia-translate: true -->
# Multi provider inválido

O runtime do Angular lançará este erro quando injetar um token destinado a ser usado com `multi: true` mas um não-Array foi encontrado. Por exemplo, `ENVIRONMENT_INITIALIZER` deve ser fornecido como `{provide: ENVIRONMENT_INITIALIZER, multi: true, useValue: () => {...}}`.
